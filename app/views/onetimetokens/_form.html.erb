<%= form_with(model: onetimetoken, local: true) do |form| %>
  <% if onetimetoken.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(onetimetoken.errors.count, "error") %> prohibited this onetimetoken from being saved:</h2>

      <ul>
      <% onetimetoken.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :onetimetoken_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :onetimetoken_content %>
  </div>

  
  <input type = "hidden" name = "token" value = "token384jdfjdsrg4hjr4uu" >
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
